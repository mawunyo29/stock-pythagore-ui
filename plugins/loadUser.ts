export default defineNuxtPlugin((nuxtApp) => {
//    const date = new Date()
// //add param to the route context 
// const currentDate = date.toLocaleDateString('fr-FR').split('/').join('-')
// const currentRoute = useRoute()
// if(currentRoute.query.date === undefined && currentRoute.path != '/'){
//   currentRoute.query.date = currentDate
// }
// nuxtApp.vueApp.config.globalProperties.$route.query = route.query
    
})
